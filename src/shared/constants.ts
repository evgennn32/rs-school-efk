const APP_STORAGE = 'https://storage.googleapis.com/english_for_kids';
const IMAGE_STORAGE = `${APP_STORAGE}/img`;
const SOUND_STORAGE = `${APP_STORAGE}/audio`;

export {APP_STORAGE, IMAGE_STORAGE, SOUND_STORAGE};
